#!/bin/bash

### This script is called after the user installs the application.

# 确保 venv 存在（install_init 可能失败）
VENV_DIR="${TRIM_PKGVAR}/app/venv"

if [ ! -d "${VENV_DIR}" ]; then
    python3 -m venv "${VENV_DIR}" 2>/dev/null || true
fi

exit 0
